/*----- constants -----*/ 


/*----- app's state (variables) -----*/ 
let board, turn, winner;


/*----- cached element references -----*/ 


/*----- event listeners -----*/ 
function init () {
  board = [
      [0,0,0],
      [0,0,0],
      [0,0,0]
  ];
  turn = 1;
  winner = null;
  render();
}

/*----- functions -----*/
function render () {
    
}